# TypeScript Refactor — Tracker

Temporary file for the JS → TS refactor. Deleted in Phase 8. Full design lives in the Claude plan; this file is the resume point.

## Status

- Phase: 5 (Web → TS, leaf-first) — **5a done** (43effe9…8088c3f), **5b done** (6970580…1510d42), **5c done** (f1a9f7e…de65d58), **5d done** (d928ffc…b5f4fb2), **5e code done** (37 commits 8199696…c50a7ea, 2026-09-19/20; ⏸ checkpoint — **dev deploy + on-device smoke still to do**)
- Step: 5e smoke. Code is complete: every file under `web/src` is TypeScript, `allowJs`/`checkJs` gone, `tseslint.configs.recommendedTypeChecked` on for web/shared/functions (test files keep it minus the idiom-fighting rules — see eslint.config.js), knip's three unused exports gone. Local smoke done 2026-09-20: the 10 public routes × desktop/tablet/mobile re-captured on the dev server (`../.playwright-mcp/p5e/`, viewport 1440/834/390) and compared to the Phase 0 baseline — identical apart from animation frames (hero mockup, demo panels, typing loops), glyph antialiasing, one sub-pixel line-wrap in Terms/mobile (JSX body byte-identical) and 1–19 px of document-height noise below the footer; browser console: only the App Check debug-token 403 for the Playwright profile and Clerk dev-key warnings.
- Last green commit: c50a7ea; `npm run check` 0 errors / 6 warnings / 626 tests (92 files); `npm run test:rules` not re-run (rules untouched since 5d: 14 tests); functions bundle 54.0 kB; knip: only `@testing-library/user-event` (Phase 6)
- Web still in JS: **none** (17 files converted in 5e)
- Dev state (2026-09-19): Phase 4 functions + rules + hosting **deployed to `cherrytree-cofounder-agree-dev`**; `deleteAccount` deleted from the project; dev SA granted `roles/firebaseappcheck.tokenVerifier`. Smoke passed: App Check + login, purchase (test card) → project + `stripeEvents/{evt}` + raw `&` in `name`, Stripe resend → `Duplicate Stripe event ignored`. **Skipped** (user, 2026-09-19): preview-PDF `&` rendering, invite/remove collaborator, contact form — see "Open from Phase 4" below.
- Next action: user "yes" → `npm run deploy:dev` (functions + hosting together), then the on-device smoke list under "Fix — Phase 5 (web)" (needs the user's Chrome profile with the registered App Check debug token and a signed-in account); then tag `refactor/p5e-done` and ⏸ before Phase 6. `origin/master` still at d12eb6c as of 2026-09-19 (checked at 5e start)
- Blocked on: the deploy confirmation. **Not yet deployed to dev**: everything from 5a on — the 5a callable change (limited-use App Check tokens + server replay rejection) must go out as functions + hosting together

### Open from Phase 4 (verify on dev before Phase 5e's full smoke)
- [ ] Preview PDF for a project whose name contains `&` (dev has `projects/org_3JXLRx9weaLVy1A7EUc59WTnj3L` = "Smith & Jones Co"): must render `&`, not `&amp;`. If Make.com treats the field as plain text, drop `escapeHtml` in `buildPdfPayload` (`functions/src/pdf.ts`).
- [ ] Invite + remove a collaborator on dev (`createOrganizationInvitation` / `removeOrganizationMember` never exercised against real Clerk after the role/redirect change)
- [ ] Contact form on dev (`sendContactMessage`)
- [x] **Stripe `cherrytree-prod-dev` endpoint** (test mode, account "Cherrytree Technologies, LLC" `acct_1Si69sEbFwh64Boe`) points at **prod's** `stripeWebhook` and prod accepts it → prod's `STRIPE_WEBHOOK_SECRET` is that *test* endpoint's signing secret, and prod's `STRIPE_SECRET_KEY` is `sk_test_` (prefix checked 2026-09-19). **User 2026-09-19: expected for now — prod runs on Stripe test mode until the real launch.** Consequence to remember: every test-card purchase on dev also reaches prod's webhook, so a plain-named test purchase creates a project + Clerk org in prod too (the Phase 4 one only failed because old prod code double-escaped `&`). Go-live steps (Stripe live-mode endpoint → prod `STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` → live price ids → redeploy) stay in "Both developers must do"; optional `livemode`-mismatch guard in `stripeWebhook` offered, not requested.

## How to resume (start of every session)

```
git status --short                      # must be empty
git branch --show-current               # refactor/ts-monorepo
sed -n '/## Status/,/## How to resume/p' docs/REFACTOR_PLAN.md
nvm use && npm ci && npm --prefix functions ci
npm run check                           # must be green (from Phase 1 on; includes typecheck from Phase 3)
npm --prefix functions run build        # lib/index.js (esbuild) — what the emulator and deploy load
```

Rules: hard stop at every `⏸` checkpoint and wait for the user's "continue". **Every new file is TypeScript (`.ts`/`.tsx`; `.mjs` only for the esbuild script) — never add `.js`/`.jsx`.** Tests are written in TS as the first step of the phase they guard (before that phase's code is touched); there is no separate test phase. One concern per commit, every commit green, no Claude attribution in commit messages. **Bugs, security gaps, nonsense and poor practice found along the way are fixed, not preserved** (user, 2026-09-18): fix each in the phase that converts that module, as its own commit with a test guarding the *corrected* behavior; when it is unclear whether or how to fix, ask the user rather than guess. Merge `origin/master` locally at the start of every phase. Never push this branch. Mid-step stop → `wip:` commit noted here, squashed by the next session.

## Phases

- [x] **Phase 0 — Baseline** — branch `refactor/ts-monorepo` off `origin/master` (`d12eb6c`); `.nvmrc`; baseline CRA build clean with 0 ESLint warnings (`.refactor-baseline/cra-build.log`); `__endpoint` golden for the 10 functions (`.refactor-baseline/endpoints.golden.json`, generated with `GCLOUD_PROJECT=test-project`); 30 baseline screenshots of the 10 public routes × desktop/tablet/mobile (`.refactor-baseline/screenshots/`). ⏸
- [x] **Phase 1 — Tooling skeleton (still JS)** — commits a04005c (move), 4576e4f (.jsx), 856fa7d (Vite), 3309b27 (ESLint), b6d2ffd (Prettier), 36efb68, 812d837 (Vitest/knip/scripts). Public routes pixel-identical to baseline (28/30 exact; 2 animation frames). `npm run check` green. Originally: 1. pure move into `web/` + root/web/shared `package.json` + workspaces; 2. pure rename `.js`→`.jsx` (`index.js`→`main.jsx`); 3. Vite 8 + `index.html` + `vite.config.ts` (port 3000) + `VITE_` env rename + `web/.env.dev`/`.env.production` + `lib/env.ts` + workflow heredocs + `firebase.json` hosting `web/dist`; verify dev, `build`, `build:dev`, no dev-bundle markers in dist; 4. ESLint 10 + Prettier, single format commit + `.git-blame-ignore-revs`; 5. Vitest 5 + RTL + jsdom + knip + root scripts + README quick-start. Full smoke. ⏸
- [x] **Phase 2 — Dead code removal** — commit c33cf81; also removed unused config helpers, CRA default logos, un-exported internal helpers; `__endpoint` golden re-verified identical. Pre-existing bug fixed with user approval in bcf7b6a (nav Sign in → `/login`). Originally: knip-verified deletes: `DynamicSection`, `DomainRedirect`, `AppRedirect`, `SectionOnboarding`, `utils/errorHandler`, `App.css`, `.Rhistory`, `.DS_Store`, client `mergeOtherFields`/`OTHER_FIELD_CONFIG`, server `crypto`/`defineString` imports, deps `web-vitals`, `env-cmd`, `react-scripts`, `firebase-functions-test`. ⏸
- [x] **Phase 3 — TypeScript foundation + `shared` package** — commits 79fe47a (tsconfigs + `typecheck` gate), e7c6e06 (`@cherrytree/shared` + 16 TS tests + move verification), ef9bd95 (web/functions consume shared; esbuild bundle; emulator lists 10; golden identical), 7d873f3 (functions vitest project). `npm run check` green (0 errors / 40 warnings / 18 tests). Deviations from the original step list: web uses the create-vite layout (`tsconfig.json` solution → `tsconfig.app.json` + `tsconfig.node.json`) so `@types/node` stays out of browser code; `shared/tsconfig.json` landed with the package commit; ack lists live in `survey/acknowledgments.ts`. Originally: 1. `tsconfig.base.json` (strict, `noUncheckedIndexedAccess`, `verbatimModuleSyntax`, `isolatedModules`, `moduleResolution: bundler`, ES2022, `skipLibCheck`; `exactOptionalPropertyTypes` off) + `web/tsconfig.json` (`allowJs: true, checkJs: false`, `@/*` alias mirrored in `vite.config.ts`, `vite-env.d.ts` with typed `ImportMetaEnv`) + `functions/tsconfig.json` (`paths` → `../shared/src`) + `shared/tsconfig.json`; root `typecheck` script in `check`. 2. `shared/` (`@cherrytree/shared`, source-exported, zero deps): `domain/` types, `time.ts`, `survey/`, `callables.ts`; tests in TS; one-off move verification. 3. Point web (workspace) and functions (esbuild alias) at shared; delete old copies; `functions/esbuild.config.mjs`, `main: lib/index.js`, `firebase.json` predeploy; temporary `functions/src/index.ts`. 4. Root vitest projects `web`, `shared`, `functions`. ⏸
- [x] **Phase 4 — Functions → TS** — commits 8647aa1 tests-first … 07ba08c rules (see Decisions log 2026-09-19); deployed to dev 2026-09-19 and smoke-tested (App Check, login, purchase, webhook idempotency; PDF `&`/invite/contact skipped → Status "Open from Phase 4"); tagged `refactor/p4-done`. Layout: `src/config.ts` (options/secrets/params/constants), `src/lib/{firebase,auth,appOrigin,validation,dates,errors}.ts`, `src/{pdf,stripe,firebaseToken,contact,organizations,clerkWebhook}.ts`, `src/index.ts`; tests co-located + `test/endpoints.test.ts` + `test/rules/`. Originally: 1. Tests first: `functions/test/endpoints.test.ts` (export set; `__endpoint` deep-equals `functions/test/endpoints.golden.json` via `createRequire` with `GCLOUD_PROJECT=test-project`; guard that every `onCall` uses a shared `CALLABLE_OPTIONS` with `invoker: 'public'` + `enforceAppCheck: true` + `consumeAppCheckToken: true`). 2. Split `index.js` directly into `functions/src/*.ts` one module per commit (`config`, `lib/{firebase,auth,validation,dates,logger,errors}`, `pdf`, `stripe`, `firebaseToken`, `contact`, `organizations`, `clerkWebhook` one handler per event, `index` re-exports) — `deleteAccount` is deleted, not ported (golden → 9 functions); the Phase 4 fixes in the triage list land as their own commits as each module is converted (auth via `request.auth`, App Check enforced, webhook idempotency/rawBody, checkout + invitation inputs server-side, `sanitizeInput` at the output boundary, rules); validation otherwise verbatim; `toErrorMessage(unknown)`; `new Stripe` + pinned `apiVersion`; `logger`; helper tests as each helper is extracted. 3. Delete the old JS files. Gate + golden + emulator; dev deploy only after explicit confirmation → purchase/webhook and preview-PDF on dev. ⏸ (fresh session)
- [ ] **Phase 5 — Web → TS, leaf-first (rename + annotate in one commit; each module's test lands first)** — **5a done 2026-09-19** (`lib/{env,firestore,firebase,functions}.ts`, `global.d.ts`, `constants/pricing.ts`, `utils/*`, `config/questionConfig.ts`, `hooks/*`, `contexts/UserContext.tsx`, fixtures in `web/src/test/fixtures/project.ts`; 17 new test files; tests were run against the JS module before each rename, then committed with it because JSDoc-inferred parameter types made the TS tests fail `typecheck` against the JS). **5b done 2026-09-19** (18 components `Standard` … `CollaboratorManager` → `.tsx`, one per commit, each with an RTL test run green against the JS first; `calculateSplit` + GROUPS/CATEGORIES extracted to `utils/equitySplit.ts`; `toErrorMessage` moved to `shared`; `ProWaitlistSignup` + typed `proWaitlist` collection; fixes: PaymentModal sessionStorage write, dead `onSuccess` prop, name minimum 2, CollaboratorManager dead support branch). **5c done 2026-09-19** (10 `Section*` + `SurveyNavigation` → `.tsx`, one per commit, tests first; shared `SurveySectionProps` + `test/sections.ts` helpers; `accelerationProtectionMonths` added to the shared schema; fixes: SectionFormation suggestion main/secondary lines, dead project-switcher chain through the pages, calculator writes need a signed-in user). **5d done 2026-09-19** (`FinalAgreement`, `Preview`, `Survey` → `.tsx`, one per commit, tests first; dedupes: `utils/getEmbedUrl.ts`, `CollaboratorsModal.tsx`, `Survey`'s section map; shared `isSectionId`; fixes: Preview submit gate counted removed collaborators, Preview showed the old submitted PDF instead of the regenerated preview, Preview's Try again reloaded the page, `useAutoSave`'s saving flag never reached `useProjectSync`, Survey's dead post-save wait and DOM-click section jump, dead `currentSection` state in Preview/FinalAgreement, AgreementHeader's unreachable expiry copy). **5e code done 2026-09-20** (15 pages + `App` + `main` → `.tsx`, one per commit, tests first; shared modules: `utils/goToDashboard.ts`, `utils/clerkTicket.ts`, `hooks/useProjectId.ts`, `hooks/useTypewriter.ts`, `hooks/useEnterpriseCardHeight.ts`, `components/FaqList.tsx`, `MARKETING_PLANS` in `constants/pricing.ts`, `dateUtils.formatTimeAgo`; fixes: EquityCalculatorPage's per-render progress-bar component and effect-built sheet, DashboardPage's unreachable Refer dialog, SurveyPage's client-clock `lastOpened`, LandingPage's always-true panel `active` prop and leaked hero timers, main's direct `import.meta.env` reads, AcceptInvitePage's unhandled signOut rejection, EquityCalculatorPage's copy confirmation; `allowJs` dropped; type-checked lint; un-exports). Originally: 5a non-component modules: `lib/firebase.ts` (cast-only `withConverter`), `lib/env.ts`, `lib/functions.ts` (`callFunction<K>`), `constants`, `utils` (tests: progressCalculation, equityCalculation, dateUtils, getPreview, collaboratorPositions), `config/questionConfig.ts` (discriminated union), `hooks` (tests: useValidation, useCollaborators, useAutoSave write-shape), `UserContext.tsx`, `Window` globals; fixtures in `web/src/test/fixtures/` typed with shared types ⏸ · 5b leaf/mid components + `calculateSplit` test ⏸ · 5c Sections + SurveyNavigation ⏸ · 5d Survey/Preview/FinalAgreement ⏸ · 5e pages/App/main, drop `allowJs`, type-checked lint, `TODO(ts-migration)` → 0, `no-explicit-any` → error. Full smoke. ⏸ (fresh session)
- [ ] **Phase 6 — Structural cleanup** — 1. Guards first: 3-layer Section harness for 10 sections + SurveyNavigation (props-contract recording snapshots; interaction tests; DOM snapshots + byte-identical `<h2 style>`/intro) with a programmatic fixture matrix; pairwise-equivalence tests for the duplicated completion rules / name helpers with known divergences pinned. 2. `SectionHeading`, `useSectionFlow`, `SurveySection`; one section per commit, zero `.snap` changes. 3. Proven dedupes; `toDate`; `goToDashboard`; lint rules back to error; react-refresh fixes; `exhaustive-deps` disables annotated. 4. Feature folders as a final pure move. `ProjectContext` deferred. ⏸
- [ ] **Phase 7 — Dependency modernization** — React 19.3; Router 6→7→8; firebase-functions 7 / admin 14 (regen golden, reviewed); `@clerk/react`, Sentry, firebase; Stripe 22 after `apiVersion` review; axios→fetch; TS bump if allowed; Tailwind 4 last/optional/screenshot-gated (user go/no-go). ⏸
- [ ] **Phase 8 — Docs, CI, hand-off** — workflows; `CLAUDE.md`/`README`/`localhost.md`/`TODO.md`; delete this file and all intermediate artifacts; final smoke on dev; `git checkout avery && git merge --ff-only refactor/ts-monorepo`; user runs `/push`. ⏸

## Decisions log

- 2026-09-19 — Phase 5e: **EquityCalculatorPage's single-click effect stays as-is** (user): the effect dispatches a synthetic `dblclick` to open a cell, but react-spreadsheet 0.10 never listens for it — it enters edit mode on a second `mousedown` on the active cell (verified against the real library in jsdom: one click → no editor, two → editor, mousedown + dblclick → no editor). Converted verbatim (typed); the test stubs `MouseEvent` because vitest's global `window` is not a jsdom `Window` wrapper. Remove it or make single-click editing real → Phase 6 / product decision.
- 2026-09-19 — Phase 5e: **AboutPage's "Apply here." stays plain text** (user): it is not a link; noted for the content owner.
- 2026-09-19 — Phase 5e: **DashboardPage's Refer a Friend dialog removed** (user): only ever `setShowReferPopup(false)`, so it could not open; the copy link also put the raw Clerk user id in a shareable URL.
- 2026-09-19 — Phase 5e: **EquityCalculatorPage fixes**: `EquityProgressBar` was declared inside the page (new component type every render → the bar remounted on each keystroke and never animated; the `static-components` warning) — now module-level with a `labels` prop; the sheet was rebuilt by an effect on `[numCofounders, cofounderNames, showCalculator]` although those only change while the form is showing — now built in the Start handler (behavior identical, effect + disable gone); the category list was a copy of `GROUPS` from `utils/equitySplit.ts` — the sheet is built from it. `columnLabels={false}`/`rowLabels={false}` dropped: the props take `string[]`, `false` fell through to the default labels and the page CSS hides the indicator cells (`tbody th { display: none }`); switching to `hideRowIndicators`/`hideColumnIndicators` would remove the indicator row and change which row the "sticky first row" CSS targets → design pass.
- 2026-09-19 — Phase 5e: **SurveyPage stamps `lastOpened` with `serverTimestamp()`** through the typed `projectRef` (was `new Date()` from the browser clock via `doc(db, …)`); rules only gate the field name, `useProjects` sorts by `toMillis()` either way.
- 2026-09-20 — Phase 5e: **LandingPage's five demo panels lose the `active` prop**: the page always rendered `<ActivePanel active={true} key={activeFeature} />` (the key remounts), so the `if (!active) reset` branches — the five `set-state-in-effect` warnings — never ran; effects start on mount and clear an effect-local timer list on unmount; PanelEquity/PanelReview data arrays are module constants (what their `exhaustive-deps` disables covered); HeroVisual's unused disable removed and its scene index is an effect local.
- 2026-09-20 — Phase 5e: **found by the type-checked lint and fixed**: AcceptInvitePage awaited `signOut()` with no handler (a rejection left the visitor on the spinner) → logs and navigates to the login URL; EquityCalculatorPage showed the "copied" check before `navigator.clipboard.writeText` resolved (and let a rejection go unhandled) → check after success, Copy stays + log on failure.
- 2026-09-20 — Phase 5e: **type-checked lint configuration**: `recommendedTypeChecked` + `projectService` (`tsconfigRootDir` = repo root); `**/*.{js,mjs}` and the root `vitest.config.ts` (no tsconfig) get `disableTypeChecked`; `no-misused-promises` with `checksVoidReturn: { attributes: false }` (async JSX handlers); tests keep the preset minus `no-floating-promises` (React 19's sync `act` returns a thenable), `no-unnecessary-type-assertion` (`expr as T` is how generic queries and hoisted mocks get their type — the auto-fix broke inference and tsc), `unbound-method` (`expect(obj.method)`), `require-await` (`act(async () => {})`), and the `no-unsafe-*` family (mocks are `any`). Eleven fire-and-forget async calls whose callee handles its own errors are `void`ed; `any` leaks closed in equityCalculation (`Array.isArray` on a readonly array narrows to `any[]` — checked on an `unknown` alias), getPreview (`text()` helper instead of `String(unknown)`: strings as-is, numbers/booleans stringified, else `''`), otherFields, LandingPage (`Array.from` for `[...Array(10)]`); pdf.ts logs the rejected Make.com URL with `JSON.stringify`.
- 2026-09-20 — Phase 5e dedupes (memory rule): `goToDashboard` → `utils/` (MarketingNav no longer exports a non-component: react-refresh warning gone; PricingPage/LandingPage copies read `import.meta.env` directly); `withClerkTicket(path, ticket)` for the four `?__clerk_ticket=` sites (encodes; a no-op for Clerk's tickets); `useProjectId()` for the three project pages (throws on a route without the parameter); `useTypewriter(text, { startDelay, charDelay, holdDelay })` for the hero line and both Protect loops (exact old timings; the hero version never cleared its timers); `useEnterpriseCardHeight()`; `FaqList({ faqs, reveal })`; `MARKETING_PLANS` (the module comment flags that this copy differs from the in-app `PRICING_PLANS`: Bootstrapped/Scale vs Starter/Pro, feature lists — content decision for the team); DashboardPage's deadline line → `formatDeadline`, its "edited … ago" arithmetic → `dateUtils.formatTimeAgo`.
- 2026-09-20 — Phase 5e: `main.tsx` reads `env.sentryDsn/isProd/enforceHttps/clerkPublishableKey` (last direct `import.meta.env` reader) and throws on a missing `#root`; `App.test.tsx` pins the route table with page stubs (factory inside `vi.hoisted`).
- 2026-09-20 — Phase 5e visual smoke recipe (for Phase 7's Tailwind gate): Playwright MCP, `browser_resize` 1440×900 / 834×1112 / 390×844, navigate, wait 2 s on animated pages, `browser_take_screenshot` fullPage + scale css; compare with `../.playwright-mcp`-side scripts (kept in the session scratchpad, easy to recreate): 4× box-downscale, count >40 blocks per 400-row band, best of ±2 px vertical offset. Antialiasing shows as ≤ ~10 blocks per page on static pages; light text on dark/photo backgrounds up to ~100; animated regions in the hundreds.

- 2026-09-19 — Phase 5d: **Preview's submit gate counts active collaborators only** (fix): `checkAllApproved` iterated every `collaborators` key, but the Clerk webhook keeps a removed member with `isActive: false` and deletes their `approvals` entry, so after anyone left, Submit stayed disabled forever while ApprovalSection (via `getSortedCollaboratorIds`) showed everyone approved. Preview now uses `useCollaborators(project).collaboratorIds`. **Server-side gap flagged** (hardening list): `submitSurvey` only checks `admin === caller`; "everyone must approve" is enforced client-side only.
- 2026-09-19 — Phase 5d: **Review & Approve shows the preview PDF while the project is editable** (user): the project effect preferred `latestPdfUrl` whenever it existed, so a submitted-but-editable project regenerated the preview after an edit and then swapped the old submitted PDF back in on the next snapshot, with no stale banner. Now `previewPdfUrl` for an editable project, `latestPdfUrl` once read-only (the submitted PDF is the Final Agreement page's job).
- 2026-09-19 — Phase 5d: **Preview's "Try again" regenerates in place** (user) instead of `window.location.reload()`.
- 2026-09-19 — Phase 5d: **AgreementHeader's unreachable expiry copy deleted** (user; open since 5c): `!isReadOnly` with a submission present means the deadline has not passed, so the "Edit window expired on …" / "You will not be able to edit …" arms never rendered. `previewPdfGeneratedAt` left the props Pick.
- 2026-09-19 — Phase 5d: **`useAutoSave` takes the saving ref it shares with `useProjectSync`** (fix): the hook set a ref of its own that it returned and nobody read; Survey handed `useProjectSync` a second, always-false ref, so the "keep the in-flight form while a save is pending" guard never fired and the write's own snapshot could revert keystrokes typed during the write. `useAutoSave(projectId, project, currentUser, isSavingRef)`; Survey is its only consumer.
- 2026-09-19 — Phase 5d: **Survey's `handlePreviewClick`** (fix, two commits): the post-save `if (saveStatus === 'saving') await 500 ms` was dead (`saveStatus` is the render-time closure value and the button is disabled whenever it is 'saving'); the jump to the first incomplete section clicked the navigation's button through `document.querySelector('[data-section-id]')` — now `goToSection(id)` (set state + `?section=`), also used by the navigation callback and Continue; `data-section-id` removed from SurveyNavigation. The simulated click's mobile-nav close never mattered (the overlay covers the button that starts the flow).
- 2026-09-19 — Phase 5d: **dead `currentSection` state in Preview and FinalAgreement removed**: it started as the page's own id and its only setter (the navigation entry for the page the user is on) wrote the same value; SurveyNavigation only compares `currentSection` against survey section ids. Both pass the constant and give the entry an explicit no-op handler (keeps it rendered; still closes the mobile nav). `GENERATED_AGREEMENT_ID` / `FINAL_AGREEMENT_ID` moved to `config/sectionConfig.ts`.
- 2026-09-19 — Phase 5d: dedupes done while converting (memory rule): `getEmbedUrl` (byte-identical in Preview and FinalAgreement) → `utils/getEmbedUrl.ts` (`(url: string) => string`; callers only reach it with a non-empty URL); the 72-line Collaborators dialog (byte-identical in Survey and Preview) → `CollaboratorsModal({ project, onClose })`, whose icon-only close button gained `aria-label="Close"`; Survey's ten `currentSection === X && …` blocks → `SECTION_COMPONENTS: Record<SectionId, ComponentType<SurveySectionProps>>` (every section now receives the optional `project`; Formation still waits for the Maps script). Survey.tsx 598 → 451 lines.
- 2026-09-19 — Phase 5d: `Shared: isSectionId(value: unknown): value is SectionId` for the `?section=` parameter (`SECTION_ORDER.includes` cannot take `string | null`); `sections.test.ts` also pins `getNextSection`/`isLastSection`.
- 2026-09-19 — Phase 5d: Survey's onboarding writes go through `projectRef` + `onboardingUpdate(userId, completed): UpdateData<Project>` for the dotted `onboardingCompleted.<uid>` path; note the SDK types nested `Record<string, …>` paths as `any` (`AddPrefixToKeys` → `string extends K ? any`), so a wrong key would not be caught — the helper is the guard. Both Preview catch blocks use `toErrorMessage`; the second `console.error` that spread the error's code/message/details was dropped.
- 2026-09-19 — Phase 5d: Preview's `isReadOnly`/`isAdmin` moved above the closures that read them — the React Compiler lint's "cannot access variable before it is declared" was one of the 16 warnings (now 15).
- 2026-09-19 — Phase 5d tests: Survey/Preview/FinalAgreement tests stub the heavy children (`SurveyNavigation`, `ApprovalSection`, `CollaboratorsModal`, `WelcomePopup`, the ten sections via a `stubSection(name)` factory inside `vi.hoisted`) recording their props, mock `useProjectSync` (Survey's mock keeps real `useState` for `formData` and records the saving ref) and render inside `MemoryRouter` with a `Location` probe; `useAutoSave`/`useValidation` run for real. `type`-only default imports (`import type SurveyNavigation from './SurveyNavigation.tsx'`) give `React.ComponentProps<typeof X>` for the recorded props without touching the mock.

- 2026-09-19 — Phase 5c: **`accelerationProtectionMonths` joins the shared schema** (user): SectionEquityVesting had always written it into surveyData (and so into the Make.com payload) without it existing in `FIELDS`/`SurveyData`/`SURVEY_FIELDS`. Now a regular string field with default `''`; no completion rule reads it (unchanged). Firestore docs gain the key as `''` on the next auto-save, like every other field.
- 2026-09-19 — Phase 5c: **dead project-switcher chain removed** (user): `SurveyNavigation` never used `allProjects`/`onProjectSwitch`/`onCreateProject` and its PaymentModal could never open; Survey/Preview/FinalAgreement only forwarded them; SurveyPage ran `useProjects` (a Firestore query per survey page load) just to feed `allProjects`; the three pages each held a PaymentModal + two handlers for it. All removed. DashboardPage keeps its live `useProjects` + PaymentModal.
- 2026-09-19 — Phase 5c: **SectionFormation suggestion rows read `placePrediction.mainText`/`secondaryText`** (fix): the code read `structuredFormat.*` (the Places REST shape); the JS API's `PlacePrediction` has the two fields directly, so every row showed the full text on line one and an empty grey line two. Test stubs `window.google.maps.importLibrary('places')` with the API's shapes.
- 2026-09-19 — Phase 5c: **equity-calculator writes require a signed-in user** (fix, same class as ApprovalSection in 5b): `handleDraftChange`/`handleModalSubmit` return without writing when `currentUser` is null instead of keying the draft/submission under `"undefined"`; `myDraft` is looked up only with an id.
- 2026-09-19 — Phase 5c: `components/sectionProps.ts` `SurveySectionProps` = `{ formData: SurveyData; handleChange: ChangeHandler; isReadOnly; showValidation; project?: CollaboratorSource }` shared by all ten sections (Survey passes the full `SurveyData`; the `|| []`/`|| {}` reads stay because a stored field can be `null` via `clearsFields`). `test/sections.ts` holds `sectionProps()` + `cardFor`/`isExpanded`/`isAnswered` for the section tests. Each section's `FIELD_ORDER` is `as const` with `Field | null` expanded state; `isAckAnswered` returns a boolean (allowed deviation).
- 2026-09-19 — Phase 5c: five `tooltip={QUESTION_CONFIG[…].tooltip}` props were removed (SectionFinal ×3, SectionDecisionMaking, SectionCompensation, SectionEquityVesting): the `satisfies` literal types show those configs declare no tooltip, so the prop was always `undefined` and Tooltip rendered nothing — DOM unchanged.
- 2026-09-19 — Phase 5c: `isNaN(string)` → `Number.isNaN(Number(value))` (what the global coerces to) and `e.target` → the typed `e.currentTarget` on an input's own handlers, with a null `selectionStart` read as 0 (JS coerced `null <= 1` to true) — SectionCompensation, SectionEquityVesting, SectionEquityAllocation. SectionCompensation's copy-pasted currency-input handlers became one set of module-level helpers used by both inputs.
- 2026-09-19 — Phase 5c: `SurveyNavigation` dropped two pieces of nonsense with the types: guards on hook functions that are always defined and the `allSectionsComplete ? 'notstarted' : 'notstarted'` dot class; the measuring canvas is a module-level `let` and a null 2D context keeps the maximum font size.
- 2026-09-19 — Phase 5c: `window.google` resolves as non-optional through `typeof globalThis` (the `?` in `global.d.ts` is overridden by the `google` namespace declaration); the runtime `window.google?.maps?.places` guard still protects. Tests remove it with `Reflect.deleteProperty(window, 'google')`.
- 2026-09-19 — Phase 5c: Section tests keep the survey-logic quirks pinned rather than fixed: an empty checkbox array / acknowledgment map is truthy for `firstUnanswered` (SectionPerformance, SectionEquityVesting, SectionIP/Final `every` on empty), so those cards never open the section even though `isAnswered` says no.

- 2026-09-19 — Phase 5b: **`PaymentModal.onSuccess` removed** (user): the modal never called it (a purchase is a full-page round trip through Stripe Checkout), so the five callers' `handlePaymentSuccess` handlers went too (`DashboardPage`, `SurveyPage`, `PreviewPage`, `FinalAgreementPage`, `SurveyNavigation`, which also lost its unused `useNavigate`).
- 2026-09-19 — Phase 5b: **project-name minimum 2 on the client** (user): mirrors `functions/src/config.ts` `PROJECT_NAME_MIN_LENGTH`; the old `< 1` check was unreachable and a one-character name reached the server's opaque "Invalid project name".
- 2026-09-19 — Phase 5b: **CollaboratorManager's "no project id → Contact Support" branch removed** (user): unreachable since 5a's `ProjectWithId`; props Pick is `id` + `editDeadline`.
- 2026-09-19 — Phase 5b: `toErrorMessage` lives in `@cherrytree/shared` (`shared/src/errors.ts`, tests moved with it); `functions/src/lib/errors.ts` keeps `toHttpsError`. Web catch blocks (`ApprovalSection`, `PaymentModal`, `CollaboratorManager`) use it instead of `err.message`.
- 2026-09-19 — Phase 5b: `calculateSplit`, `GROUPS`, `CATEGORIES` extracted from `EquityCalculatorModal` to `web/src/utils/equitySplit.ts` (typed on `EquityCalculatorDraft`) — the Phase 6 react-refresh item for this file, done while converting it; the modal keeps its own `previewSplit` (neutral-score-1 variant).
- 2026-09-19 — Phase 5b: `questionConfig.ts` splits the radio/dropdown union: `RadioQuestion` (string or value/label options) and `DropdownQuestion` (value/label only, what `CustomSelect` takes — both shipped dropdowns already comply). `QuestionRenderer` narrows `config` to top-level questions (`otherField` must be a `SurveyFieldName`; the nested cofounder questions are rendered by `SectionCofounders`) and reads the field value as `unknown` with three readers (text / string list / ack map) reproducing the JS fallbacks. `clearsFields` still writes `null` for non-acknowledgment fields (survey logic; cast with a comment).
- 2026-09-19 — Phase 5b: `MarketingNav.goToDashboard` reads `env.appUrl` (was a direct `import.meta.env` read); it stays exported from the component module until the Phase 6 hook.
- 2026-09-19 — Phase 5b: `AgreementHeader` uses the shared `toDate()` for `pdfAgreements[].generatedAt` (typed `StoredDate`, a bare `.toDate()` does not compile; identical for the Timestamp the client receives). `ApprovalSection` writes through the typed `projectRef` and returns null when there is no signed-in user after loading (the old path would have written an `approvals.undefined` key).
- 2026-09-19 — Phase 5b: `CollaboratorManager` treats Clerk's optional `publicUserData`/`userId` defensively and only renders Remove when a userId exists (always the case for a membership).
- 2026-09-19 — Phase 5b: component tests use RTL `fireEvent` + `vi.hoisted` mocks (`../lib/firebase`, `../lib/functions`, `../contexts/UserContext`, `@clerk/clerk-react`, `firebase/firestore`); `Object.defineProperty(window, 'location', …)` works in this jsdom for the redirect tests; date-sensitive tests pin `vi.setSystemTime` or use a far-future `editDeadline` (the fixture's 2026-07-01 default has already passed).

- 2026-09-19 — Phase 5a: **`updatedAt` removed, not written** (user): `lastUpdated` is set at creation and on every auto-save, so the dashboard's `lastUpdated || updatedAt || createdAt` never reached it. Dropped from `Project`, the rules allowlist (rules test asserts rejection) and `DashboardPage`.
- 2026-09-19 — Phase 5a: **App Check replay rejection in all 7 callables** (user): `functions/src/lib/appCheck.ts` `rejectConsumedAppCheckToken` → `permission-denied`; `requireAuth` calls it, `getFirebaseToken`/`sendContactMessage` call it directly; `test/endpoints.test.ts` counts one guard per `onCall`. Client side `web/src/lib/functions.ts` `callFunction<K>` sends `{ limitedUseAppCheckTokens: true }`. **Deploy functions + hosting together**: a client on the old bundle reuses one standard token for ~1 h and would be rejected on its second call.
- 2026-09-19 — Phase 5a: `callFunction` guards the Firebase session with `await auth.authStateReady()` + `auth.currentUser` (throws `SESSION_NOT_READY_MESSAGE` before any network call) for every callable except `getFirebaseToken` and `sendContactMessage`; `UserContext` keeps `firebaseAuthReady` internal.
- 2026-09-19 — Phase 5a: **truthy → boolean** (user, allowed deviation): `useValidation.isSectionCompleted` / `isOtherFieldValid` / `isOtherArrayFieldValid` and `progressCalculation`'s helpers return `!!(...)`; every consumer only used them as conditions. The two "Other" helpers were byte-identical in both files and are now imported from `progressCalculation.ts` (the *section rules* stay duplicated — Phase 6 equivalence tests).
- 2026-09-19 — Phase 5a: **`organizationList` removed** from `UserContext` and `SurveyPage`: Clerk 5's `useOrganizationList` no longer returns it (Core 2 → `userMemberships`), so it was always `undefined` and `SurveyPage`'s active-org effect never called `setActive`; `Survey.jsx` already does it from `userMemberships`.
- 2026-09-19 — Phase 5a: `equityCalculation` lost its Firestore `row_N/col_N` converter and `collaboratorIds` option — only `EquityCalculatorPage` calls it, with the react-spreadsheet matrix and no options (dead paths; tests exercised them against the JS first).
- 2026-09-19 — Phase 5a: `useProjectSync` sets `project.id` (`ProjectWithId`); `Survey`/`Preview` no longer spread `{ ...project, id: projectId }` into `CollaboratorManager`.
- 2026-09-19 — Phase 5a: `lib/firebase.ts` exposes `projectRef(id)`/`userRef(id)` on cast-only converters (`lib/firestore.ts`); `useAutoSave` writes through it but pins its update object with a local `AutoSaveUpdate` type and casts to `UpdateData<Project>` once — Firestore's `UpdateData` cannot express `Record<string, object>` survey fields (verified with both interface and type-alias shapes).
- 2026-09-19 — Phase 5a: `config/questionConfig.ts` is `{...} satisfies Partial<Record<QuestionFieldName, QuestionConfig>>` so each entry keeps its literal shape for consumers; `QuestionConfig` is the union `text|textarea|number|date` / `radio|dropdown` / `checkbox` / `acknowledgment` / `custom`. Keys include the nested cofounder fields (`keyof Cofounder`).
- 2026-09-19 — Phase 5a: `lib/env.ts` centralizes `import.meta.env` (Firebase config object, boolean flags parsed from the literal `'true'`); `main.jsx` and the marketing pages still read `import.meta.env` directly until 5e. Removed `VITE_MARKETING_URL`, `VITE_STRIPE_PUBLISHABLE_KEY` and the dead `VITE_STRIPE_*_PRICE_ID` lines from `.env.example`, `vite-env.d.ts` and all three workflow heredocs.
- 2026-09-19 — Phase 5a: `web/src/global.d.ts` declares `Window.google` (via `@types/google.maps`, now an explicit devDependency and a tsconfig `types` entry), `Window.Tally`, and `var FIREBASE_APPCHECK_DEBUG_TOKEN` (mirrors the App Check SDK's internal declaration).
- 2026-09-19 — Phase 5a: web TS modules import each other with explicit `.ts`/`.tsx` extensions (matches `shared`/`functions`); the remaining `.jsx` files import them extensionless until converted.

- 2026-09-19 — Phase 4: **golden contract changes** (deliberate, in `functions/test/endpoints.golden.json`): `deleteAccount` gone (9 functions); `submitSurvey`/`generatePreviewPDF` no longer bind `CLERK_SECRET_KEY` (they read `request.auth.uid` only — least privilege). `createCheckoutSession` keeps it: the customer email comes from Clerk (`getClerkPrimaryEmail`) rather than `request.auth.token.email`, so a Firebase Auth user created before the Clerk webhook existed still checks out.
- 2026-09-19 — Phase 4: **non-secret params** `APP_ORIGIN`, `STRIPE_STARTER_PRICE_ID`, `STRIPE_PRO_PRICE_ID` via `defineString` in committed `functions/.env.<projectId>` files (`.gitignore` un-ignores exactly those two). Price ids were already public in the client bundle. Prod file holds placeholders until someone with the live ids fills it (see "Both developers must do").
- 2026-09-19 — Phase 4: **redirect origins**: `resolveAppOrigin` honours the request `Origin` only when it equals `APP_ORIGIN` or `http://localhost:3000`, else falls back to `APP_ORIGIN`. Keeps the localhost → dev-functions workflow working without trusting the header.
- 2026-09-19 — Phase 4: **Stripe idempotency** = `stripeEvents/{eventId}` marker written after success + 500 on failure (Stripe retries). Not transactional: Stripe does not deliver one event concurrently, and a marker written *before* processing would swallow a retry after a crash. Known residual: a failure between Clerk org creation and the Firestore write leaves an orphan Clerk org on retry (logged).
- 2026-09-19 — Phase 4: **Clerk webhook** also returns 500 on handler failure (membership handlers used to swallow errors → silent missing collaborator). `emailVerified` now from `verification.status` (the old `.verified` field never existed → always false). Not in the original fix list; both are "nonsense → fix".
- 2026-09-19 — Phase 4: `sanitizeInput` → `normalizeProjectName` (NUL strip, trim, cap 100, min 2; stored raw) + `escapeHtml` only in the Make.com payload `projectName`. The old `javascript:`/`data:`/`on*=` stripping is dropped (meaningless once nothing is treated as HTML; the client still rejects those patterns). **Verify on dev** that Make.com renders `&` from `&amp;` — if it treats fields as plain text, drop the escape.
- 2026-09-19 — Phase 4: invitation email format check kept as the verbatim regex (not `isValidEmail`) to avoid a behavior change; Clerk validates for real. Clerk error messages no longer pass through to the client (`internal` + fixed message). Possible UX follow-up: map `ClerkAPIResponseError.errors[0].longMessage` (e.g. "already a member") to `already-exists`.
- 2026-09-19 — Phase 4: **rules** beyond the triage item (all tested): `users` write → false (no client writer; a user could rewrite their own `stripeCustomerId`); project access requires `collaborators[uid].isActive == true` (removed members kept read/write via their history entry); project `delete` → false (no client path); `proWaitlist` create constrained to `{email, timestamp, source}` with types/lengths; dead chat-agent rule removed. `stripeEvents` covered by deny-all.
- 2026-09-19 — Phase 4: `consumeAppCheckToken: true` kept (tracker decision) but it is only replay protection once the client sends limited-use tokens **and** handlers reject `request.app.alreadyConsumed`; today the SDK only flags it. → Phase 5a: `callFunction<K>` passes `{ limitedUseAppCheckTokens: true }`; then add the `alreadyConsumed` check to `requireAuth`. Until then it costs one App Check API call per request for nothing.
- 2026-09-19 — Phase 4: App Check on the dev server uses `self.FIREBASE_APPCHECK_DEBUG_TOKEN = true` (SDK-minted token, stored per browser profile, registered once in the dev console) — no token in env files or git.
- 2026-09-19 — Phase 4: no `lib/logger.ts`; modules import `logger` from `firebase-functions` directly (a re-export module added nothing). `functions/test/helpers/fakeFirestore.ts` is the in-memory collection used by handler tests; handler tests call `fn.run(request)` (callables) or `fn(req, res)` (webhooks) with `svix`/`stripe`/`@clerk/backend`/`resend`/`axios` mocked.
- 2026-09-19 — Phase 4: rules tests run only under `npm run test:rules` (`firebase emulators:exec --only firestore --project demo-cherrytree`); the root vitest config adds the `rules` project only when `FIRESTORE_EMULATOR_HOST` is set. Needs Java (present here, Java 25) and downloads the emulator jar on first run.
- 2026-09-19 — Phase 4 deploy: the functions run as the custom least-privilege SA `cloud-functions@<project>.iam.gserviceaccount.com` (roles: `datastore.user`, `firebaseauth.admin`, `iam.serviceAccountTokenCreator`, `serviceusage.serviceUsageConsumer`). It had never been able to verify App Check tokens (`app-check/permission-denied`); harmless while unenforced, fatal once enforced. **Granted `roles/firebaseappcheck.tokenVerifier` on dev**; prod needs the same before its deploy (see "Both developers must do"). Phase 8: document the SA role set.
- 2026-09-19 — Phase 4 deploy: `firebase deploy` aborted on the first run because `deleteAccount` still existed remotely (non-interactive CLI refuses to delete); it was removed with `firebase functions:delete deleteAccount --region us-west2` and the deploy re-run. Prod will hit the same — the prod workflow needs `--force` or an explicit delete step (Phase 8 workflow item).
- 2026-09-19 — Phase 4 deploy: Stripe accounts — dev's `STRIPE_SECRET_KEY` belongs to **"Cherrytree Technologies, LLC"** (`acct_1Si69sEbFwh64Boe`, sandbox/test mode), which holds the `cherrytree-dev` endpoint (`…cloudfunctions.net/stripeWebhook`). "Cherrytree, LLC" (`acct_1UHlbcH5jV6VeEcj`) and its "Cherrytree sandbox" are not used by any environment. Phase 8: document the canonical account; consider deleting the unused sandbox.

- 2026-09-18 — **Plan revised**: the standalone JS "characterization tests" phase was double work (write JS tests, then rename/retype). Now: TypeScript foundation first (Phase 3), every test written once in TS as the first step of the phase it guards, no new JS files ever. Phases renumbered 3–8.

- 2026-09-18 — Phase 3: web TS config uses the create-vite layout (`web/tsconfig.json` is a solution file referencing `tsconfig.app.json` for `src` and `tsconfig.node.json` for `vite.config.ts`/`vitest.config.ts`). Reason: mirroring the `@/*` alias in `vite.config.ts` needs `node:url`, and adding `@types/node` to the app project would leak `process`/`Buffer` and make `setTimeout` return `NodeJS.Timeout` in browser code.
- 2026-09-18 — Phase 3: `typecheck` runs `tsc -p` per project (web app, web node, shared, functions) rather than `tsc -b`; `*.tsbuildinfo` git-ignored in case someone runs `-b`.
- 2026-09-18 — Phase 3: `tsconfig.base.json` also carries TS 6.0's recommended `noUncheckedSideEffectImports`, `moduleDetection: force`, `allowImportingTsExtensions` (all projects are `noEmit`), and an explicit `lib: ["ES2023"]` so `shared`/`functions` never see DOM types (web adds `DOM`, `DOM.Iterable`).
- 2026-09-18 — Phase 3: `@types/node` pinned to the 22.x line at the root and in `functions` (matches the runtime; the transitive copy was 26.x). `@types/react`/`@types/react-dom` at 19.2.x to match React 19.2.3 (bump with React in Phase 7).
- 2026-09-18 — Phase 3: `ImportMetaEnv` declares the 18 `VITE_*` keys as `string` (Vite convention; a missing key is still `undefined` at runtime). Unknown keys resolve to `any` because Vite's own `ImportMetaEnv` has a string index signature — a typo in an env key is not a type error.
- 2026-09-18 — Phase 3: `shared` internal imports use explicit `.ts` extensions so the package also runs under plain Node (type stripping) for one-off scripts; consumers import the bare package name.
- 2026-09-18 — Phase 3: option lists are typed `readonly string[]`, not literal unions — stored survey values can be anything (merged "Other" text, older options), so `SurveyData` string fields are `string`.
- 2026-09-18 — Phase 3: `Project` fields written at creation by `stripeWebhook` are required, later-written fields optional, stored `surveyData` is `Partial<SurveyData>`. Reads stay defensive; loosen if legacy documents lacking creation fields turn up.
- 2026-09-18 — Phase 3: `mergeOtherFields` ported with `Array.isArray` guards and one shared helper for the identical top-level/nested branches. Only divergence (malformed data): an array-typed field holding a string that contains "Other" used to throw `TypeError` (→ `internal` HttpsError); it is now left untouched. Server `OTHER_FIELD_NAMES` not ported (unused). `SECTIONS` display copy initially moved with `sectionConfig.js` although only web reads it; **moved back** to `web/src/config/sectionConfig.ts` in 0599087 (presentation stays out of `shared`).
- 2026-09-18 — Phase 3: `CallableMap` covers all 8 `onCall` functions, including `deleteAccount` (no client caller today).
- 2026-09-18 — Phase 3 checkpoint review (user): (1) `SECTIONS` UI copy → web (done, 0599087); (2) keep `Project` creation-time fields required — nothing is in prod yet, no legacy documents to loosen for; (3) `mergeOtherFields` malformed-data divergence accepted as-is; (4) dropping server `OTHER_FIELD_NAMES` confirmed.
- 2026-09-18 — Phase 3: functions bundle: esbuild `external` = keys of `functions/package.json` `dependencies` (esbuild externalizes their subpaths too, verified: `firebase-functions/v2/https` etc. stay `require()`s); `@cherrytree/shared` inlined via `alias`; `absWorkingDir` so the build is cwd-independent. Firebase `predeploy` runs it; **CI must install functions devDependencies (`npm --prefix functions ci`) before `firebase deploy`** — Phase 8 workflow item.

- 2026-09-18 — Phase 1: Prettier config `singleQuote: true, printWidth: 100`, `*.md` and `web/public` ignored. Format commit verified render-neutral (CSS byte-identical; JS diff = unquoted keys + JSX whitespace normalization; two double spaces in `TermsPage` collapsed to one, invisible under normal whitespace).
- 2026-09-18 — Phase 1: lint rules `no-unused-vars` (with `^_` ignore) and `no-case-declarations` downgraded to warn during migration; React-Compiler rules from react-hooks v7 as warn. Flip all to error in Phase 6. 46 warnings tracked (40 after Phase 2).
- 2026-09-18 — Phase 1: `firebase` CLI stays a global install (knip `ignoreBinaries`), not a devDependency — keeps the team workflow unchanged.
- 2026-09-18 — Phase 1: `@testing-library/user-event` adopted at 14.x (no tests existed on 13.x); Vitest `globals: true`.
- 2026-09-18 — Phase 1: the dead `crypto` require was removed early (it shadowed Node's global and was the only lint error).
- 2026-09-18 — Phase 1: `@clerk/clerk-react` is deprecated upstream in favour of `@clerk/react` (Clerk Core 3) → Phase 7 item.

- 2026-09-18 — TypeScript 6.0.x, not 7.0: `typescript-eslint` peer range is `<6.1.0`. Configs written TS-7-clean.
- 2026-09-18 — npm workspaces for `web` + `shared` only; `functions/` stays a standalone package (own lockfile) so `firebase deploy` keeps its deterministic `npm ci` path.
- 2026-09-18 — Functions bundled with esbuild directly (tsup is in maintenance mode), CommonJS output, `@cherrytree/shared` inlined via alias.
- 2026-09-18 — No zod in this refactor; existing validation ported verbatim. zod goes to the hardening phase.
- 2026-09-18 — Vite custom mode `dev` (`web/.env.dev`) so `vite build --mode dev` is a production bundle with dev keys, matching CRA's `build:dev`.
- 2026-09-18 — `__endpoint` golden covers memory/region/secrets/service account/trigger type. `invoker` and `consumeAppCheckToken` are not in the manifest in firebase-functions 6.x → guarded by a source-level test on the shared callable options constant.
- 2026-09-18 — `ProjectContext` (single listener) deferred: `useProjectSync` echo suppression is per-caller.
- 2026-09-18 — No `<StrictMode>`; Sentry `environment` semantics kept; missing env vars stay silently undefined (Clerk key still throws).

## Allowed non-behavioral deviations (user-confirmed once, applied everywhere)

- [x] server `console.*` → `firebase-functions` `logger.*` with identical messages (Phase 4)
- [x] `Stripe(...)` → `new Stripe(...)` with `apiVersion: '2025-10-29.clover'` (the stripe 19.3 default — requests unchanged) (Phase 4)
- [x] validation predicates return `boolean` (`!!`) instead of the last truthy operand (Phase 5a, `useValidation` + `progressCalculation` helpers)
- [ ] removal of dead imports/files and unused dependencies
- [ ] Prettier formatting (single commit, in `.git-blame-ignore-revs`)

## Both developers must do (after this branch lands)

- `nvm install 22 && nvm use` (tooling needs ≥22.22)
- Move/rename local env files: `.env.development` → `web/.env.dev`, `.env.production` → `web/.env.production`, and rename every key `REACT_APP_*` → `VITE_*` (values unchanged)
- `npm ci && npm --prefix functions ci` (functions now needs its devDependencies — esbuild — for `firebase deploy`'s predeploy build)
- Commands: `npm run dev` (was `npm start`), `npm run check` before pushing
- **App Check debug token (Phase 4):** on first `npm run dev` the browser console prints `App Check debug token: …`; register it in the **dev** Firebase console → App Check → Apps → web app → Manage debug tokens. Once per browser profile. Until then every callable returns `unauthenticated` on localhost.
- **Prod params (Phase 4):** fill `functions/.env.cherrytree-cofounder-agreement` with the LIVE Stripe price ids (placeholders today) before the next prod deploy; `APP_ORIGIN` is already set.
- **Prod IAM (Phase 4):** the functions runtime service account `cloud-functions@cherrytree-cofounder-agreement.iam.gserviceaccount.com` needs `roles/firebaseappcheck.tokenVerifier` before the next prod deploy — with App Check enforced, every callable rejects until it can verify tokens (`app-check/permission-denied` in the logs). Granted on dev 2026-09-19 via `gcloud projects add-iam-policy-binding <project> --member=serviceAccount:cloud-functions@<project>.iam.gserviceaccount.com --role=roles/firebaseappcheck.tokenVerifier`.
- `npm run test:rules` (Firestore rules under the emulator) needs Java on the machine
- **GitHub secrets no longer read (Phase 5a):** `DEV_/PROD_MARKETING_URL`, `DEV_/PROD_STRIPE_PUBLISHABLE_KEY`, `DEV_/PROD_STRIPE_STARTER_PRICE_ID`, `DEV_/PROD_STRIPE_PRO_PRICE_ID` can be deleted from the repo settings once this branch lands (harmless if left)
- **Deploy functions and hosting together (Phase 5a):** the web app now sends limited-use App Check tokens and the functions reject a reused one; `npm run deploy:dev` does both, the prod workflow must too
- **Prod deploy (Phase 4):** the deploy must delete the retired `deleteAccount` function (`firebase functions:delete deleteAccount --region us-west2` or `firebase deploy --force`).
- **Stripe go-live (deferred by the user until the real launch; prod is on test mode today):** in the business account, Live mode → Developers → Webhooks → add endpoint `https://stripewebhook-bcygoc2oqq-wl.a.run.app` for `checkout.session.completed`; then `firebase functions:secrets:set STRIPE_SECRET_KEY --project cherrytree-cofounder-agreement` (`sk_live_…`), `firebase functions:secrets:set STRIPE_WEBHOOK_SECRET --project cherrytree-cofounder-agreement` (that endpoint's `whsec_…`), fill the live price ids in `functions/.env.cherrytree-cofounder-agreement`, disable the test-mode `cherrytree-prod-dev` endpoint, redeploy functions via the prod workflow.

## Found during refactor — triaged 2026-09-18

Rule (user, 2026-09-18): bugs, security gaps and poor practice found while refactoring are **fixed**, not preserved — each as its own commit with a test guarding the corrected behavior. Scope: the *broader* stuff (auth, webhooks, rules, config, dead code). **Survey logic stays as-is for now** (validation/completion rules, field schema quirks, "Other" merging, display names, ordering). Abuse/scale hardening (rate limits etc.) is deferred — few users today. Ask for confirmation when unsure.

### Fix — Phase 4 (functions + rules)
- [x] **App Check enforced**: `enforceAppCheck: true` on every callable; web initializes App Check in all modes (debug token for localhost/dev, reCAPTCHA v3 in prod). Currently `consumeAppCheckToken: true` without enforcement (`functions/index.js`, `functions/organizations.js`, `web/src/firebase.js`)
- [x] **Callables use `request.auth`**: web already signs into Firebase with a Clerk-minted custom token (`UserContext.jsx`), so `request.auth.uid` is available; delete the Clerk-session-token-in-body path (`functions/auth-helpers.js`) and the client code that sends it
- [x] **`deleteAccount` deleted** (no client caller; nondeterministic admin transfer; sets Clerk role `'admin'` vs `'org:admin'` check). Remove from `CallableMap` and the endpoint golden (→ 9 functions)
- [x] **Stripe webhook idempotent** on `event.id`; return 5xx on inner failure so Stripe retries (today: duplicate Clerk org + project on retry, 200 on failure)
- [x] **Clerk webhook verifies `req.rawBody`**, not `JSON.stringify(req.body)`
- [x] **`createCheckoutSession`**: server-side `plan → priceId` map; `successUrl`/`cancelUrl` from config, not the client
- [x] **`createOrganizationInvitation`**: `redirectUrl` from config, not the `Origin` header; role forced to member server-side
- [x] **`sanitizeInput`**: store raw; HTML-escape only when building the Make.com PDF payload (today `Acme & Co` → `Acme &amp; Co` in Firestore/Clerk/Stripe)
- [x] **Contact-form submitter email no longer logged**
- [x] **Firestore rules**: `projects` update locks `collaborators`/`payments`/`editDeadline`/`pdfAgreements`/`currentPlan` to server/admin; `list` gets a field constraint — verify client write/query paths first (`firestore.rules`)
- [x] `console.*` → `logger.*`; pinned Stripe `apiVersion` (allowed deviations, tick when applied)

### Fix — Phase 5 (web)
- [x] `useProjectSync` sets `project.id` (5a, ac23211)
- [x] `lastOpened` → `serverTimestamp()` (5e, 778cbe2); ~~`updatedAt` written on every project save~~ → **removed** instead (5a, bc48198; decision above)
- [x] `migrateCollaboratorPositions` (documented no-op) removed (5a, 6f41257)
- [x] `VITE_MARKETING_URL` / `VITE_STRIPE_PUBLISHABLE_KEY` removed from `web/.env.example`, `vite-env.d.ts` and the workflows (5a, 3d2eca0; the workflows' dead `VITE_STRIPE_*_PRICE_ID` lines went too)
- [x] `lib/functions.ts` `callFunction<K>` passes `{ limitedUseAppCheckTokens: true }` (5a, 488bf41) and the server rejects `request.app.alreadyConsumed` in every callable (5a, 148e263) — **needs the 5e dev deploy to take effect**
- [x] `PaymentModal` `sessionStorage.paymentStartTime` is written and never read — removed (5b, 2d4c1b9); dead `onSuccess` prop removed (5b, dd18e67); name minimum aligned to the server's 2 (5b, 3f1c177)
- [x] `CollaboratorManager` unreachable "no project id" support branch removed (5b, 1510d42)
- [x] `SectionFormation` suggestion rows read the REST `structuredFormat` shape instead of the JS API's `mainText`/`secondaryText` (5c, 15fd067)
- [x] Dead project-switcher chain (`allProjects`/`onProjectSwitch`/`onCreateProject`, never-opened PaymentModals, `useProjects` query in SurveyPage) removed (5c, 635ea2f)
- [x] `SectionEquityAllocation` calculator writes keyed under `"undefined"` without a user → guarded (5c, 8ab691d)
- [x] `accelerationProtectionMonths` written by `SectionEquityVesting` but absent from the shared schema → added (5c, f9d06d1)
- [x] `AgreementHeader` dead copy inside the "Last submitted on …" branch — deleted (5d, 26dfcf3; user decision)
- [x] `Preview.checkAllApproved` counted removed collaborators → Submit disabled forever after anyone left (5d, 2b31f44)
- [x] `Preview` preferred the old submitted PDF over the regenerated preview while editable (5d, 66df0f5); "Try again" reloaded the page (5d, 9306654)
- [x] `useAutoSave`'s saving flag never reached `useProjectSync` — keystrokes during a write could be reverted by its snapshot (5d, e286b47)
- [x] `Survey.handlePreviewClick` dead 500 ms wait (5d, 5581b15) and DOM-click section jump (5d, a402615)
- [x] Dead `currentSection` state in `FinalAgreement` (5d, 59c5c21) and `Preview` (5d, b71818b)
- [ ] **Phase 5e on-device smoke (after `npm run deploy:dev`)** — the three checks skipped in Phase 4 (Status → "Open from Phase 4") **plus**: App Check + login on the dev server (debug token) and on dev hosting (reCAPTCHA v3); every callable works with limited-use tokens; a second click after a failed call is not rejected as a replay; the 5d behaviors on a real project: Review & Approve after removing a collaborator, the preview vs. submitted PDF on an editable submitted project, Try again, Survey's jump to the first incomplete section, keystrokes during a save; the 5e behaviors: dashboard cards (edited-ago, deadline, Continue's cover animation), equity calculator (progress bar animates now; Copy confirms after the write), contact form (validation message vs fallback), accept-invite while signed in
- [x] 5e fixes with tests: EquityCalculatorPage progress bar (51cf92b) + sheet build (51171fb); DashboardPage Refer dialog (e88ea82); LandingPage panels (79de8b0) + hero timers (f71c86d); main via `lib/env` (8ac648f); AcceptInvitePage signOut rejection (301d440); EquityCalculatorPage copy confirmation (d14388c)
- [x] `UserContext.organizationList` / `SurveyPage` active-org effect — dead since Clerk Core 2 (found in 5a, removed 7190e56)
- [ ] `UserContext` has no error state when the Clerk → Firebase exchange fails: `loading` stays `true` forever (pinned by `UserContext.test.tsx`). Surface an error / retry — hardening phase (UX change)
- [x] Callables wait for the Firebase session: `callFunction` awaits `auth.authStateReady()` and throws before the call when nobody is signed in (5a, 488bf41)

### Fix — Phase 8 (docs)
- [ ] `CLAUDE.md` data model stale: field is `surveyData` not `formData`; `users` has no `name`/`organizationIds`; no `organizations` collection; chat-agent references obsolete; `EquityCalculator.js`/`DynamicSection` wrong; still documents `functions/surveySchema.js` and `src/config/surveySchema.js`/`sectionConfig.js`
- [ ] `TODO.md` ESLint-warnings list stale (baseline build has 0 warnings)
- [ ] `.claude/commands/localhost.md` says `npm start` (CRA) and references the retired chat agent
- [ ] `CLAUDE.md` (web app) still documents `functions/index.js`, `auth-helpers.js`, `organizations.js` and the "Auth Flow" via Clerk JWT in callables; now `functions/src/*.ts`, Firebase session via `getFirebaseToken`, App Check enforced, params in `functions/.env.<projectId>`, `npm run test:rules`
- [ ] Workflows: `firebase deploy` now needs `npm --prefix functions ci` (esbuild) and picks up `functions/.env.<projectId>`; document the App Check debug-token step in README
- [ ] Document infra not derivable from the repo: functions runtime SA `cloud-functions@<project>` and its role set (incl. `firebaseappcheck.tokenVerifier`); canonical Stripe account/sandbox per environment and which webhook endpoint each env owns; where each secret in Secret Manager comes from
- [ ] `firebase-functions@^6.6` flagged outdated by the CLI at deploy time → Phase 7 item already covers the v7 upgrade; just make sure the golden is regenerated there

### Deferred — survey logic, leave as-is (user, 2026-09-18); pin current behavior in Phase 6 tests
- `FIELDS` declares `compensations` as `userId/name/amount/frequency`; code stores `{who, amount}`; `FIELDS.COMPENSATION_*` unused (`shared/src/survey/fields.ts`, `SectionCompensation`)
- `firstUnanswered` treats an empty acknowledgment map `{}` as answered (`every` on empty) while `isAckAnswered` treats it as unanswered (all `Section*` components); likewise `!formData[f]` treats an empty checkbox array / ack map as answered (SectionPerformance, SectionEquityVesting, SectionDecisionMaking) — commented in the .tsx, pinned by the 5c tests
- `accelerationProtectionMonths` is required by the UI (red asterisk under acceleration = Yes) but by no completion rule, so the section can be "complete" with the asterisk showing
- `SectionFormation.handleSelectAddress` only overwrites `mailingStreet2` when the chosen place has a subpremise (an old suite number survives a new address)
- `mergeOtherFields` mutates nested cofounder objects in place through a shallow copy (`shared/src/survey/otherFields.ts`; pinned by a shared test)
- `finalEquityPercentages` declared in the schema but never written
- Completion rules diverge between `useValidation.isSectionCompleted` and `progressCalculation.countCompletedSections` (zero-collaborator acks; equity `0` vs `''`) — do not dedupe
- `getCollaboratorName` (SectionEquityAllocation) vs `useCollaborators().getDisplayName` diverge for inactive/unknown users — do not replace one with the other
- `collaboratorPositions.getSortedCollaboratorIds` sorts by name although its doc says "by join time"
- `INITIAL_FORM_DATA` default arrays/objects shared by reference through `getInitialFormData()` = `{ ...INITIAL_FORM_DATA }`
- `SURVEY_FIELDS.hasOther`/`otherField`/`options` metadata not consumed (the "Other" merge is driven by `OTHER_FIELD_CONFIG`; options by `questionConfig`)

### Deferred — abuse/scale hardening (few users today)
- `sendContactMessage` unauthenticated and un-rate-limited
- **`submitSurvey` does not check approvals** (found in 5d): the server only verifies `project.admin === caller`; "every active collaborator must approve" lives in `Preview.checkAllApproved` (client) only, so an admin calling the callable directly generates the final PDF with zero approvals. Fix server-side: require `approvals[id] === true` for every collaborator with an open `history` period (the same set `isActiveCollaborator` uses), with a test

## Phase 6 lint follow-ups (from the Phase 1 lint run)

- Flip `@typescript-eslint/no-unused-vars` and `no-case-declarations` back to `error`; ~~fix `useValidation.js` `const`-in-`case`~~ done in 5a (each case has its own block). 21 warnings left after 5a (all in `.jsx`)
- Remove unused `eslint-disable` directive in `LandingPage.jsx`
- `react-refresh/only-export-components`: ~~move `calculateSplit` (EquityCalculatorModal)~~ done in 5b (`utils/equitySplit.ts`); ~~`goToDashboard` (MarketingNav)~~ done in 5e (`utils/goToDashboard.ts`); still to move: `useUser` (UserContext). 19 warnings left after 5b
- React-Compiler warnings (`set-state-in-effect` ×11, `immutability`, `static-components`) stay as warnings → hardening phase
- knip: `@testing-library/user-event` is for Phase 6 interaction tests (5b/5c tests used `fireEvent`); ~~`calculateSplit` export~~ consumed by its 5b test
- Structural (found in 5c, left for the Phase 6 harness): every section repeats the `FIELD_ORDER`/`expandedField`/`advanceTo`/`collapse` state machine and four repeat `isAckAnswered` → `useSectionFlow`; SectionDecisionMaking inlines two copies of the per-collaborator acknowledgment checklist that QuestionRenderer's acknowledgment branch already renders; SectionEquityVesting and SectionEquityAllocation each define `isValidPercentage`; `SurveyNavigation` opens its own `useProjectSync` Firestore listener on the project Survey/Preview/FinalAgreement already listen to (`ProjectContext` item)
- 16 warnings left after 5c; 15 after 5d; **6 after 5e** (every page-level one fixed for real, not silenced): `set-state-in-effect` in Preview ×2, Survey ×1 (URL → section sync), SurveyNavigation, UserContext; react-refresh `useUser` (UserContext). `no-unused-vars`/`no-case-declarations` back to `error` is a one-line flip now (no offenders)
- Type-checked lint is on (5e). Rules left off for test files (`no-floating-promises`, `no-unnecessary-type-assertion`, `unbound-method`, `require-await`, `no-unsafe-*`) could be revisited with `allowForKnownSafeCalls` for `act` once typescript-eslint handles the assertion-as-inference case
- Dead/odd things left as-is by user decision in 5e: EquityCalculatorPage's single-click `dblclick` effect (never worked — react-spreadsheet needs a second mousedown); AboutPage's "Apply here." plain text
- Design pass items from 5e: EquityCalculatorPage hides react-spreadsheet's indicator row/column with CSS instead of `hideRowIndicators`/`hideColumnIndicators` (the "sticky first row" CSS targets the hidden indicator row); marketing `MARKETING_PLANS` copy vs in-app `PRICING_PLANS` (Bootstrapped/Scale vs Starter/Pro, different feature lists, `$2,000` vs `$2000`)
- Structural (found in 5d): Survey and Preview each inline the same fixed top header (Back to Dashboard, save status / "Saved h:mm", `UserButton`) with different container classes — a `SurveyHeader` once the harness exists; Survey's scroll-to-first-error still queries `.text-red-700, .validation-error` through the DOM (needs a ref-based hand-off from the sections); Preview keeps `pdfUrl` in state although it is derivable from the project once the server's `previewPdfUrl` write lands

## Intermediate artifacts to delete in Phase 8

- `.refactor-baseline/` (build log, endpoint golden source copy, screenshots) — local only, excluded via `.git/info/exclude`
- `../.playwright-mcp/` (Playwright MCP snapshots + the `p5e/` re-capture set; lives in the parent `Cherrytree/` folder, outside the repo)
- The `.git/info/exclude` entries themselves
- `docs/REFACTOR_PLAN.md` (this file)
- Local tags `refactor/p<N>-done`
