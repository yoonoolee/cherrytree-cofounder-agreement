# Non-secret Cloud Functions parameters for the PROD project (see src/config.ts).
# Loaded by the Firebase CLI at deploy time. Secrets never go here:
# firebase functions:secrets:set NAME
#
# TODO before the next prod deploy: replace the placeholders with the LIVE Stripe Price ids
# (Stripe Dashboard > Product catalog; they were VITE_STRIPE_*_PRICE_ID in web/.env.production).
APP_ORIGIN=https://my.cherrytree.app
STRIPE_STARTER_PRICE_ID=price_REPLACE_WITH_LIVE_STARTER_PRICE_ID
STRIPE_PRO_PRICE_ID=price_REPLACE_WITH_LIVE_PRO_PRICE_ID
